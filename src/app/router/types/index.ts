/**
 * Router types barrel export
 * Import from "@/app/router/types" for convenience
 */

export { RouteNames } from "./names";
export type { AppRouteRecordRaw, MenuItem, RouteMeta } from "./types";
